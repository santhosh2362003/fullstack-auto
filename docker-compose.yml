version: '3.8'
services:
  db:
    image: mongo:latest
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - DB_URL=mongodb://db:27017/mytask
    depends_on:
      - db
  frontend:
    build: ./frontend
    ports:
      - "80:80"
